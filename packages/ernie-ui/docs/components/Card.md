# Card

Bordered container that groups related content under an optional 24px title. Most screens need no cards at all; use one only when two groups of content must share a screen.

```tsx
<Card title="Reminders">
  <Switch label="Remind me every Sunday?" checked={on} onChange={setOn} />
</Card>
```
