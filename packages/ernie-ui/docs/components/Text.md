# Text

Body paragraph at 20px with 1.5 line height and a 65-character measure. Use `size="large"` (24px) for the one sentence the user must read. One idea per paragraph, short sentences, no jargon.

```tsx
<Text size="large">Tap the person you want to call.</Text>
<Text>You can change your mind on the next screen.</Text>
```
